log timestamp precision 6

vrf red
 exit-vrf
!
interface r1-eth0
 ip address 192.0.2.1/24
exit
!
router bgp 65000
 no bgp ebgp-requires-policy
exit
!
router bgp 65300 vrf red
 no bgp ebgp-requires-policy
exit
!
